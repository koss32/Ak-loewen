# Релиз 2 — передача следующему разработчику / ИИ

> Исторический документ от 14.09.2026. Описывает ветку `release-2` на момент этого релиза, а не текущую рабочую ветку. Актуальный маршрут: `release-6`, корень приложения `site/`; см. [README](../../README.md).

**Историческая рабочая ветка релиза:** `release-2`. **Код этого релиза:** `site/` в той ветке.

Основан на `codex/release-a`, коммит `3e8cd4f7eeb272f6237d2fab49a9d99712fb836b`. Релиз A не заменять и не перезаписывать. Не путать этот релиз с историческими v2-концептами проекта. Владелец одобрил итоговый просмотр 14 сентября 2026 года.

## Что сделано

1. **Тренеры.** По уточнению владельца Namig Aliyev ведёт бокс, Anar Karimov — самбо и ММА. Добавлены тексты о знакомстве с новичками. Чемпионство Каримова среди спортсменов до 20 лет сообщено владельцем, независимо не проверялось. Дополнительные регалии не придуманы. Прежняя иллюстрация Каримова восстановлена с пометкой, что это не настоящее фото; фотография Алиева сохранена.
2. **Первое занятие.** Добавлен блок для родителей: специальную экипировку заранее покупать не нужно, достаточно спортивной одежды; тренер встретит ребёнка и поможет освоиться в группе. Сохранено одно бесплатное посещение.
3. **Единая заявка.** VALSET использует тот же серверный Telegram-приём, что AK Löwen. Instagram — дополнительная связь, не обязательный этап. Один выбранный контакт; время и комментарий необязательны. Обе серверные реализации используют `src/trial-message.js`. Возраст, группа, согласие и защита от повторов сохранены в коде.
4. **Анимации.** Восстановлено прежнее изображение, добавлен `public/scroll-motion.js`: последовательное появление блоков, отдельный вход тренеров, глубина портретов и индикатор прокрутки. Учтены мобильные устройства, клавиатура и reduced-motion. Ориентиры: предоставленный ScrollCraft и `Leonxlnx/taste-skill`. Движок не заменён.
5. **Локализации.** Изменения внесены в DE/RU/UK/TR. Новые тексты: `family-copy.js`, `first-visit-copy.js`, `booking-copy.js`.

## Просмотр и сборка

Автономный файл: `concepts/ak-loewen-valset-release-2.html`. Открывается без установки, начинается на русском, **не отправляет заявки**.

Для сборки: Node.js 24+, `cd site && node build.js`. Результат: `dist/ak-loewen-valset-release-2.html` и локализованные страницы.

## Важные ограничения

Это сохранение исходников, не публикация рабочего сайта. Секреты и настройки хостинга не менялись; реальные заявки не отправлялись. Тесты и браузерные проверки по просьбе владельца не перезапускались; исторические тесты не обновлялись. Успешные проверки релиза A нельзя приписывать релизу 2.

Для реального приёма нужны существующие серверные настройки Telegram, хранилище журнала, утверждённые юридические тексты и отдельное разрешение владельца на публикацию. Не включать доставку или развёртывание автоматически. Исторические документы v1/A не отменяют согласованные изменения этого файла.
